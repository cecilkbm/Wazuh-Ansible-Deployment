[defaults]
remote_user = admin
host_key_checking = false
inventory = inventory
collections_paths = /home/admin/.ansible/collections:/usr/share/ansible/collections

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
